services:
    webapp:
        image: ysof_student
        build:
            context: ./
            dockerfile: Dockerfile
        restart: unless-stopped
        container_name: ysof_student_${NODE_ENV}
        ports:
            - "3001:80"